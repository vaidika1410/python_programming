a = (3, 14, 8, 1)

print(a)

# a[2] = 15 --> not allowed 

# creating a single element tuple
# a tuple cannot have single element like this (3), because parenthesis in python are used for grouping things together

num = (3, )
print(num)