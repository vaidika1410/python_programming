# write a function that takes first name and last name as parameters and returns the full name

def full_name(first, last):
    # return first + " " + last
    return f"{first} {last}"

name = full_name('Vaidika', 'Kaul')
print(name)